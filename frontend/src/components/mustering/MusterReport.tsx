// MusterReport — list of past events + per-event report detail + CSV export.
// Phase 4 (Sonnet) fills this in. This is a clean slot stub.

export default function MusterReport() {
  return (
    <div className="rounded-lg border border-dashed border-gray-300 dark:border-gray-700 p-8 text-center text-gray-500 dark:text-gray-400">
      <p className="font-medium">Report</p>
      <p className="text-sm mt-1">Past muster events and CSV export — coming in Phase 4.</p>
    </div>
  );
}
