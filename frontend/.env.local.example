# -----------------------------------------------------------------------------
# TrakRF Handheld Application Configuration
# -----------------------------------------------------------------------------
# 
# For development with mocked BLE:
#   1. Set BLE_MCP_* variables for your bridge server
#   2. Run: pnpm dev:mock
#
# For E2E testing:
#   - Tests ALWAYS use the mock (browser security requirement)
#   - Configure BLE_MCP_* variables for your test environment
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------------------
# Backend API URL (optional, defaults to /api/v1)
# For development with backend on different host/port:
# VITE_API_URL=http://localhost:8080/api/v1
# For production, leave unset to use relative path /api/v1

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Log levels: fatal, error, warn, info, debug, trace, silly
VITE_LOG_LEVEL=debug

# -----------------------------------------------------------------------------
# RFID Configuration
# -----------------------------------------------------------------------------
# Default RF Power (dBm) - can be overridden in settings
VITE_DEFAULT_RF_POWER=30.0

# Default Session - can be overridden in settings  
VITE_DEFAULT_SESSION=1

# Connection timeout in milliseconds
VITE_CONNECTION_TIMEOUT=10000

# Inventory scan timeout in milliseconds
VITE_INVENTORY_TIMEOUT=30000

# -----------------------------------------------------------------------------
# Development Settings
# -----------------------------------------------------------------------------
# Enable debug features in development
VITE_ENABLE_DEBUG_PANEL=true

# Mock device for development (when no real CS108 available)
VITE_MOCK_DEVICE=false

# Enable performance monitoring
VITE_ENABLE_PERFORMANCE_MONITORING=false

# -----------------------------------------------------------------------------
# Feature Flags
# -----------------------------------------------------------------------------
# Enable barcode scanning features
VITE_ENABLE_BARCODE=true

# Enable locate features
VITE_ENABLE_TAG_SEARCH=true

# Enable reconciliation features
VITE_ENABLE_RECONCILIATION=true

# Enable dark mode by default
VITE_DEFAULT_DARK_MODE=true

# -----------------------------------------------------------------------------
# BLE Mock/Bridge Configuration (when using ble-mcp-test)
# -----------------------------------------------------------------------------
# Bridge server configuration (simplified naming after consolidation)
BLE_MCP_HOST=localhost               # Bridge server host
BLE_MCP_WS_PORT=8080                # Bridge server WebSocket port
BLE_MCP_HTTP_PORT=8081              # Bridge server HTTP/MCP port
BLE_MCP_HTTP_TOKEN=                 # Optional auth token
BLE_MCP_LOG_LEVEL=info              # Bridge server log level

# CS108 Device Configuration
BLE_DEVICE_NAME=CS108               # Device name filter
# BLE_SESSION_ID=my-custom-session  # Optional: Override auto-generated session ID

# Enable BLE mock for development/testing
# VITE_BLE_MOCK_ENABLED=true

# Note: The bridge URL is now built automatically from components:
# WebSocket URL = ws://${BLE_MCP_HOST}:${BLE_MCP_WS_PORT}
# No need to manually set VITE_BLE_BRIDGE_URL anymore

# -----------------------------------------------------------------------------
# OpenReplay Session Replay (optional)
# -----------------------------------------------------------------------------
# Enable OpenReplay session recording
VITE_OPENREPLAY_ENABLED=false

# OpenReplay project key (get from OpenReplay dashboard at app.openreplay.com)
# VITE_OPENREPLAY_PROJECT_KEY=your_project_key_here

# OpenReplay ingest endpoint (only needed for self-hosted, not for cloud version)
# For cloud version, leave this commented out - it will use the default cloud endpoint
# VITE_OPENREPLAY_INGEST_POINT=https://openreplay.your-domain.com/ingest

# Allow insecure mode for local development (only set to true for localhost)
# Not needed for cloud version which uses HTTPS by default
# VITE_OPENREPLAY_INSECURE=false

# Application version for OpenReplay metadata
VITE_APP_VERSION=1.0.0

# -----------------------------------------------------------------------------
# GitHub Integration (optional)
# -----------------------------------------------------------------------------
# GitHub token for PR and push activity
# GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# E2E Testing Timeouts (milliseconds)
# -----------------------------------------------------------------------------
BRIDGE_CONNECT_TIMEOUT=5000
BRIDGE_COMMAND_TIMEOUT=2000
BRIDGE_UI_TIMEOUT=3000
BRIDGE_SCAN_TIMEOUT=10000