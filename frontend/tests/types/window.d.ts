/**
 * Test-specific window extensions
 * These properties are attached to window during E2E tests
 */

// Import the main window types (types are globally available via declaration merging)
import '../../src/types/window';

export {};